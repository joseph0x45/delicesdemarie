package components

import "shop/models"

templ Index(products []models.Product) {
	@Layout(main(products))
}

templ main(products []models.Product) {
	<script>
  function handler() {
    return {
      cart: [],
      addToCart(id) { },
      removeFromCart(id) { },
      saveOrder() { }
    }
  }
</script>
	<main x-data="handler"></main>
}
